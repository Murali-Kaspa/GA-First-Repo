name : first Gituhub Action
on : workflow_dispatch
jobs:
  first-job:
    runs-on : ubuntu-latest
    steps:
      - name : Greet
        run : echo "HEllo"
      - name : ODne
        run : echo "Bye"
